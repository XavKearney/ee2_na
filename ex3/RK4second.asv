function [ q_next,i_next ] = RK4second(h,L,R,C,q0,i0,qi,iL,ti,vin)
    % q = x
    % iL = q'
    fq = @(q,i) i;
    fi = @(q,i) vin - q/C - R*i;
    
    k1_q = h*fq(qi,iL);
    k2_q = h*fq(qi+h/3,iL+k1_q/3);
    k3_q = 
    
    
    
    
    q_next = qi + (k1_q+3*k2_q+3*k3_q+k4_q)/8;
    i_next = iL + (k1_i+3*k2_i+3*k3_i+k4_i)/8;


end

